<?xml version="1.0" encoding="utf-8" ?>
<!--Copyright NuoBiT Solutions - Frank Cespedes <fcespedes@nuobit.com>
License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl).-->
<odoo>
    <template
        id="auth_signup_validation_portal_my_details_fields"
        inherit_id="portal.portal_my_details_fields"
    >
        <xpath expr="//div[contains(@t-attf-class, 'state_id')]" position="after">
            <div class="clearfix" />
            <div
                t-attf-class="mb-3 #{error.get('languages') and 'o_has_error' or ''} col-xl-6"
            >
                <label class="col-form-label" for="language">Language</label>
                <select name="language" class="form-control" required="True">
                    <t t-foreach="languages" t-as="lang" t-key="lang[0]">
                        <option
                            t-att-value="lang[0]"
                            t-att-selected="lang[0] == (partner and partner.lang or request.env.context.get('lang', 'en_US'))"
                        ><t t-esc="lang[2].split('/').pop()" /></option>
                    </t>
                </select>
            </div>
        </xpath>
    </template>
</odoo>
