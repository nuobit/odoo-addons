<?xml version="1.0" encoding="utf-8" ?>
<!--Copyright NuoBiT Solutions - Kilian Niubo <kniubo@nuobit.com>
    Copyright NuoBiT Solutions - Frank Cespedes <fcespedes@nuobit.com>
License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl).-->
<odoo>
    <template
        id="portal_my_home_menu_documents"
        name="Portal layout: documents menu entries"
        inherit_id="portal.portal_breadcrumbs"
        priority="20"
    >
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li
                t-if="page_name == 'document' or document"
                t-attf-class="breadcrumb-item #{'active' if not document else ''}"
            >
                <a
                    t-if="document"
                    t-attf-href="/my/documents?{{ keep_query() }}"
                >Documents</a>
                <t t-else="">Documents</t>
            </li>
            <li t-if="document" class="breadcrumb-item active">
                <span t-field="document.document_type_id.name" />
            </li>
        </xpath>
    </template>

    <template
        id="portal_my_home"
        name="Show Documents"
        customize_show="True"
        inherit_id="portal.portal_my_home"
        priority="40"
    >
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Documents</t>
                <t t-set="url" t-value="'/my/documents'" />
                <t t-set="placeholder_count" t-value="'document_count'" />
            </t>
        </xpath>
    </template>

    <template id="portal_my_documents" name="My Documents">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True" />

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Documents</t>
            </t>
            <t t-if="not documents">
                <div class="alert alert-warning mt8" role="alert">
                    There are no Documents.
                </div>
            </t>
            <t t-if="documents" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>
                            <span class='d-none d-md-inline'>Document type</span>
                        </th>
                        <th class="d-none d-md-table-cell">
                            <span class='d-none d-md-inline'>Description</span>
                        </th>
                        <th class="text-center">
                            <span class='d-none d-md-inline'>Expiration Date</span>
                        </th>
                        <th class="text-center">
                            <span class='d-none d-md-inline'>Expired</span>
                        </th>
                    </tr>
                </thead>
                <tr t-foreach="documents" t-as="document">
                    <td>
                        <a
                            t-attf-href="/my/documents/#{document.id}?{{ keep_query() }}"
                        ><span t-field="document.document_type_id.name" /></a>
                    </td>
                    <td class="d-none d-md-table-cell">
                        <span t-field="document.description" />
                    </td>
                    <td class="text-center">
                        <span t-field="document.expiration_date" />
                    </td>
                    <td class="text-center">
                        <t t-if="document.expired">
                            <i
                                class="fa fa-exclamation-triangle text-danger"
                                aria-hidden="true"
                                title="The file has expired"
                            />
                        </t>
                    </td>
                </tr>
            </t>
        </t>
    </template>

    <template
        id="portal_document_page"
        name="Document Portal Template"
        inherit_id="portal.portal_sidebar"
        primary="True"
    >
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <div id="portal_sale_content">
                <div t-call="partner_document_portal.partner_document_portal_content" />
            </div>
        </xpath>
    </template>

   <template
        id="partner_document_portal_content"
        name="Partner Document Portal Content"
    >
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12">
                <t t-if="request.params.get('error')">
                    <div class="alert alert-danger" role="alert">
                        <strong>Error:</strong>
                        <t t-if="request.params.get('error') == 'attachment_failed'">
                            An error occurred while uploading the attachment. Please try again.
                        </t>
                    </div>
                </t>
                <form
                        t-attf-action="/my/documents/update_document/{{ document.id }}"
                        method="post"
                        enctype="multipart/form-data"
                        class="col-lg-7"
                    >
                    <input
                            type="hidden"
                            name="csrf_token"
                            t-att-value="request.csrf_token()"
                        />
                    <div id="introduction" class="form-group mb-3 ">
                        <h3>
                            <t t-out="document.document_type_id.name" />
                        </h3>
                    </div>
                    <t t-if="document.document_type_id.template_id">
                        <t
                                t-set="template_file"
                                t-value="document.get_template_file_by_lang()"
                            />
                        <t t-if="template_file">
                            <div class="form-group mb-3">
                                <div
                                        class="border p-3 rounded bg-light d-flex justify-content-between align-items-center"
                                    >
                                    <div>
                                        <label class="font-weight-bold">
                                            Template:
                                        </label>
                                        <span class="ml-2">
                                            <t t-esc="template_file.template_id.name" />
                                        </span>
                                    </div>
                                    <a
                                            t-att-href="template_file.get_portal_url(suffix='/download', download=True)"
                                            class="btn btn-info"
                                        >
                                        <i class="fa fa-download" /> Download Template
                                    </a>
                                </div>
                                <div class="alert alert-info mt-3" role="alert">
                                    Please download the template, fill it in, and then upload the completed file below.
                                </div>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="alert alert-warning" role="alert">
                                The template for this language is not available. Please contact support or change the language.
                            </div>
                        </t>
                    </t>
                    <div class="form-group mb-3">
                        <label class="font-weight-bold">Description:</label>
                        <input
                                type="text"
                                name="description"
                                class="form-control"
                                t-att-value="document.description"
                                required="required"
                            />
                    </div>
                    <t t-if="not document.document_type_id.template_id">
                        <div class="form-group mb-3">
                            <label class="font-weight-bold">Expiration Date:</label>
                            <input
                                    type="date"
                                    name="expiration_date"
                                    class="form-control"
                                    t-att-value="document.expiration_date"
                                    required="required"
                                />
                        </div>
                    </t>
                    <div class="form-group mb-3">
                        <label class="font-weight-bold">Validated:</label>
                        <t t-if="document.validated">
                            <span class="text-success">Validated</span>
                        </t>
                        <t t-if="not document.validated">
                            <span class="text-danger">Not Validated</span>
                        </t>
                    </div>
                    <div class="form-group mb-3">
                        <label class="font-weight-bold">Attachments:</label>
                        <t t-if="document.datas">
                            <span class="ml-2"><t t-esc="document.datas_fname" /></span>
                            <div class="border p-3 rounded bg-light">
                                <a
                                        t-att-href="document.get_portal_url(suffix='/download', download=True)"
                                        class="btn btn-info"
                                    >
                                    <i class="fa fa-download" /> Download Attachment
                                </a>
                            </div>
                        </t>
                        <t>
                            <div class="form-group mt-3">
                                <input
                                        type="file"
                                        name="attachment"
                                        class="form-control"
                                    />
                            </div>
                        </t>
                    </div>
                    <button type="submit" class="btn btn-primary float-end mb32">
                        Confirm
                    </button>
                </form>
            </div>
        </div>
    </div>
</template>
</odoo>
