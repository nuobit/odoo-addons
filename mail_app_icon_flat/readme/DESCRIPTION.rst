Glue module between ``web_app_icon_flat`` and ``mail``: it makes Odoo 16 show
the flat app icons, instead of the Odoo 16 ones, in:

* the activities menu of the top bar;
* the messages menu of the top bar, next to the messages on a record.
