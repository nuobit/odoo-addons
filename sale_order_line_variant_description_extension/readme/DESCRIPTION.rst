This module extends the OCA module sale_order_line_variant_description and makes
it compatible with having internal product references between brackets. Also
replaces the template sale description with the product variant description.
