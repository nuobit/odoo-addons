<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="product_template_form_view" model="ir.ui.view">
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <field name='product_variant_count' position="after">
                <field name='has_variants' invisible='1' />
            </field>
        </field>
    </record>

    <record id="product_template_only_form_view" model="ir.ui.view">
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <field name="default_code" position="attributes">
                <attribute
                    name="attrs"
                >{'invisible': [('has_variants', '=', True)]}</attribute>
            </field>
            <button name='%(product.product_variant_action)d' position="attributes">
                <attribute
                    name="attrs"
                >{'invisible': [('has_variants', '=', False)]}</attribute>
            </button>
        </field>
    </record>
</odoo>
